import React, { ReactNode } from 'react';

import { Styled{{pascalCase name}} } from "./{{pascalCase name}}.styles";

type {{pascalCase name}}PropsType = {

}

export default function {{pascalCase name}}({}: {{pascalCase name}}PropsType): ReactNode {
  return (
    <Styled{{pascalCase name}}>
      {{pascalCase name}}
    </Styled{{pascalCase name}}>
  );
};